name: grits-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12.7
  - numpy
  - pandas
  - pytorch
  - scikit-learn
  - transformers  # if using ModernBERT
  - pip
  - pip:
      - pytest
      - pytest-cov
